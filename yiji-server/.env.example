# 复制为 .env 后修改（.env 已被 .gitignore 忽略，不会进入仓库）
PORT=8787
# 务必改成一段足够长的随机字符串（可用 `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` 生成）
JWT_SECRET=change-me-to-a-long-random-string
# 数据存放目录（运行时生成，已忽略）
DATA_DIR=./data
# 允许跨域的来源；本地联调用 *，生产改成你的前端域名
CORS_ORIGIN=*
